# build directories
build*/
__pycache__

# Temporary testing files
tst/test_log.txt

# Athena output files
*.tab
*.vtk
*.bin
*.athdf*
*.hst
*.rst

# Mac OS metadata
.DS_Store
.vscode

# editor temp files
*~
*.swp
Lorene

# python cache
__pycache__/

# compressed bin archives (Dropbox-only, too large for git)
validation/bin/

# the papers the documents cite (Dropbox-only): published PDFs, ~22 MB, not ours to
# redistribute and permanent repo weight
docs/

# movies
validation/*.mp4
# LaTeX build products (validation documents)
validation/*.aux
validation/*.log
validation/*.out
validation/*.toc
validation/*.fls
validation/*.fdb_latexmk
validation/*.synctex.gz

# per-run output directories (frozen input + provenance + data; not versioned).
# validation/run/ is the local data home (all phases); runs/ covers the cluster
# scripts' layout in cluster clones.
runs/
validation/run/
