# build directories
build*/
__pycache__

# Temporary testing files
tst/test_log.txt

# Athena output files
*.tab
*.vtk
*.bin
*.athdf*
*.hst
*.rst

# Mac OS metadata
.DS_Store
.vscode

# editor temp files
*~
*.swp
Lorene

# python cache
__pycache__/

# compressed bin archives (Dropbox-only, too large for git)
validation/bin/

# LaTeX build products (validation documents)
validation/*.aux
validation/*.log
validation/*.out
validation/*.toc
validation/*.fls
validation/*.fdb_latexmk
validation/*.synctex.gz

# per-run output directories (frozen input + provenance + data; not versioned).
# The pattern is unanchored, so it covers validation/runs/ (local analysis mirrors)
# and a top-level runs/ (the cluster scripts' layout) alike.
runs/
