# AthenaK input file: gravito-turbulent stratified shearing box, the standard
# beta = Omega*t_cool = 10 run of Shi & Chiang (2014), ApJ 789, 34 (their tc=10).
#
# Physics: 3D hydro shearing box (q = 3/2, Omega = 1) with linearized vertical tidal
# gravity, self-gravity (multigrid: shear-periodic x1 + periodic x2 + slab-open x3),
# and constant-beta cooling dU/dt = -U*Omega/beta.  Code units rho0 = H = Omega = 1
# with SC14's fiducial constants: gamma = 5/3, Toomre Q0 = 1 initial equilibrium,
# cs(0) = 2.12625, G = 0.3384 (four_pi_G = 4.25231).
#
# Box [-32H,32H]^2 x [-6H,6H] at 512 x 512 x 96 -- SC14's ".hi" resolution
# (8 cells/H), 32^3 cubic MeshBlocks = 16 x 16 x 3 = 768 blocks.
#
# WHY 32^3 AND NOT 16^3: the multigrid root grid is one cell per MeshBlock and is
# solved REPLICATED on every rank, so its cost is serial -- it does not fall with
# added nodes.  16^3 blocks here would give a 32x32x6 = 6144-cell root grid (8x
# this one) and, measured at fixed mesh and rank count, a 1.45x slower gravity
# solve; the gap widens with rank count.  32^3 also halves the ghost-cell
# overhead (1.67x vs 2.6x of the active volume at nghost=3).
#
# Recommended launch: 256 ranks = ONE filled Athena Turin node (3 blocks/rank),
# or 384 ranks (2 blocks/rank).  Do NOT go past ~768 ranks: there are only 768
# blocks, and NAS charges whole nodes whether or not their cores are working.
#
# This file is the beta = 10 ANCHOR of the hi-res chain: restarts cannot cross
# resolutions, so tc = 5/4/3 morph from THIS run's final restart (they are
# mutually independent and can run concurrently); see
# scripts/cluster/gt_sc14_beta_chain.sh.
#
# SC14 Table 1 targets at THIS resolution (their ".hi" rows), for the chain:
#   tc=10.hi  dT=300  alpha_Reyn 1.70e-2  alpha_grav 3.82e-2  alpha 5.52e-2
#                     alpha' 4.06e-2  dv 1.79 HOm  <cs> 2.17
#   tc= 5.hi  dT=200  4.19e-2  5.82e-2  10.0e-2  8.19e-2  2.20  2.01
#   tc= 4.hi  dT=200  5.69e-2  6.50e-2  12.2e-2  10.0e-2  2.31  1.96
#   tc= 3.hi  dT= 20  FRAGMENTS (no steady state; their collapse criterion is
#                     t_cool <~ 3/Omega -- tc=4 surviving is the prediction test)
# Time averages are taken over the trailing dT_avg = 100/Omega of each run.
#
# History columns (user_hist, see src/pgen/fluids/gravito_turb.cpp):
#   mass, rho_cs, rho_wgrv, rho_wrey, rho_prs, wgrv, wrey, rho_cs2, rho_dv2, eint
#   Q      = (rho_cs/mass)*omega0/(pi*G*mass/(Lx*Ly))
#   alpha  = (rho_wgrv + rho_wrey)/rho_prs          (SC14 eq. 19)
#   alpha' = (2/3)*(wgrv + wrey)/rho_cs2            (SC14 eq. 20;
#            rho_cs2 = gamma*int P absorbs eq. 20's 1/gamma)
#   rms dv = sqrt(rho_dv2/mass)

<comment>
problem   = SC14 gravito-turbulence (beta = 10)

<job>
basename  = GravitoTurbHi

<mesh>
nghost    = 3         # Number of ghost cells
nx1       = 512       # Number of cells in X1-direction (SC14 ".hi": 8 cells/H)
x1min     = -32.0     # minimum value of X1
x1max     = 32.0      # maximum value of X1
ix1_bc    = shear_periodic  # Inner-X1 boundary condition flag
ox1_bc    = shear_periodic  # Outer-X1 boundary condition flag

nx2       = 512       # Number of cells in X2-direction
x2min     = -32.0     # minimum value of X2
x2max     = 32.0      # maximum value of X2
ix2_bc    = periodic  # Inner-X2 boundary condition flag
ox2_bc    = periodic  # Outer-X2 boundary condition flag

nx3       = 96        # Number of cells in X3-direction
x3min     = -6.0      # minimum value of X3
x3max     = 6.0       # maximum value of X3
ix3_bc    = diode     # no-inflow outflow (plain outflow feeds a gravity-driven
ox3_bc    = diode     # inflow runaway through the z faces; SC14's "outflow" intent)

<meshblock>
nx1       = 32        # cubic MeshBlocks (multigrid requirement); see header
nx2       = 32
nx3       = 32

<time>
evolution  = dynamic  # dynamic/kinematic/static
integrator = rk2      # time integrator
cfl_number = 0.3      # The Courant, Friedrichs, & Lewy (CFL) Number
nlim       = -1       # cycle limit
tlim       = 300.0    # time limit (300/Omega, SC14 standard duration)
ndiag      = 100      # cycles between diagnostic output

<hydro>
eos         = ideal   # EOS type
gamma       = 1.6666666666666667
reconstruct = plm     # spatial reconstruction method
rsolver     = hllc    # Riemann-solver to be used
dfloor      = 1.0e-4  # density floor (SC14: 1e-4 rho0)
pfloor      = 1.0e-10 # pressure floor (halo shocks; SC14 used Athena's default floors)

<shearing_box>
qshear    = 1.5       # shear rate parameter q
omega0    = 1.0       # rotation frequency
stratified = true     # vertical tidal gravity -Omega^2 z
orbital_advection = true

<gravity>
solver    = multigrid # fft | multigrid
four_pi_G = 4.25231   # = 4*pi*0.3384 (SC14 fiducial G)
mg_bc     = slab      # slab-open (vacuum) x3 boundaries
mg_remap  = ppmx      # shear ghost + slab plane remap order
threshold = -1.0      # FIXED iteration count (see below), not automatic control:
niteration = 6        # the stagnation rule polishes the defect to ~1e-11 at 3x the
                      # cost of 6 warm-started V-cycles (~1e-7, far below the 1e-4
                      # discretization error) -- and the V-cycle count multiplies the
                      # REPLICATED coarsest-grid work that extra ranks cannot reduce
full_multigrid = false  # iterative mode: warm-start from the previous potential
show_defect = 0

<hydro_srcterms>
self_gravity = true
beta_cooling = true
bcool_beta   = 10.0   # beta = Omega*t_cool
thermal_cooling = false  # SC14 "b" runs: t_cool = tcool_b*(rho/P)^3 (set
tcool_b      = 200.0     # beta_cooling=false when enabling)
cool_eps     = 0.02      # dt <= cool_eps*t_cool

<problem>
pgen_name = gravito_turb
user_hist = true
rho0      = 1.0
cs0       = 2.12625   # midplane sound speed (Q0 = 1 fiducial)
dfloor_ic = 1.0e-4    # initial-condition halo floor
pert_amp  = 0.1       # velocity perturbations up to pert_amp*cs0 ...
pert_zmax = 2.0       # ... for |z| < pert_zmax
pert_seed = 1

<output1>
file_type = hst
dt        = 0.25

<output2>
file_type = bin
variable  = hydro_w
dt        = 5.0

<output3>
file_type = bin
variable  = grav_phi
dt        = 5.0

<output4>
file_type = rst
dt        = 25.0
